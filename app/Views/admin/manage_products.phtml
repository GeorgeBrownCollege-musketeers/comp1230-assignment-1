<?php
    $itemModel = model('App\Models\ItemModel');
    $items = $itemModel->getItems();
    $header = $itemModel->getHeader();
?>
<h1>Manage Products</h1>

<table>
    <thead>
        <tr>
            <?php foreach($header as $heading):?>    
                <th><?= $heading ?></th>
            <?php endforeach ?>
        </tr>
    </thead>
    <tbody>
        <?php foreach($items as $item):?>    
            <tr>
                <?php foreach($item as $key=>$value):?>  
                    <?php if ($key == 'image'): ?>
                        <td><img width="100px" src="/<?= $value ?>" alt="<?= $value ?>"></td>
                    <?php else: ?>
                        <td><?= $value ?></td>
                    <?php endif ?>
                <?php endforeach ?>
            </tr>
        <?php endforeach ?>
    </tbody>
</table>
<table>
    <thead>
        <tr>
            <?php foreach($header as $heading):?>    
                <th><?= $heading ?></th>
            <?php endforeach ?>
        </tr>
    </thead>
    <tbody>
        <?php foreach($items as $item):?>    
            <tr>
            <form action="">
                <?php foreach($item as $key=>$value):?>  
                    <?php if ($key == 'image'): ?>
                        <td><img width="100px" src="/<?= $value ?>" alt="<?= $value ?>"></td>
                    <?php else: ?>
                        <td><input type="text" name="<?=$key."-".$value?>" value="<?= $value ?>"></td>
                    <?php endif ?>
                <?php endforeach ?>
                <td>
                    <input type="submit" value="submit">
                </td>
            </form>
            </tr>
        <?php endforeach ?>
    </tbody>
</table>


<h2>Add Product</h2>
<form method="POST" action="add_product" id="add-product-form">
    <input type="text" name="item_name" placeholder="Name">
    <input type="text" name="item_category" placeholder="Category" >
    <input type="text" name="item_price" placeholder="Price" >
    <input type="text" name="item_image" placeholder="Image" >
    <input type="text" name="item_rating" placeholder="Rating" >
    <input type="text" name="item_quantity" placeholder="Quantity" >
    <input type="submit">
</form>

